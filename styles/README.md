# Styles

This project keeps most shared styling in `app/globals.css` through Tailwind and CSS custom properties.
Keep future extracted tokens or editor-specific styles in this folder if the design system grows.
